// 7. Write a program that calculates a number of digits of a given number.

fuction numDigits (a) {
    var b = a.toString();
    var result = b.length;
    return result;
}
console.log(numDigits(2));